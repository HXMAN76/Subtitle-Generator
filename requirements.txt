# Core dependencies
moviepy>=1.0.3
faster-whisper>=1.0.0  # 3-4x faster than openai-whisper
torch>=2.0.0
torchaudio>=2.0.0
torchvision 
tqdm
pydub>=0.25.1

# NMT dependencies
sentencepiece>=0.1.99
sacrebleu>=2.3.0
nltk>=3.8.0

# Audio processing
ffmpeg-python>=0.2.0

# API Backend
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Silero VAD (loaded from torch.hub)
# No package needed - loaded via torch.hub.load()

# For accessing HF datasets/models (optional, for data download)
# datasets

# Evaluation metrics (optional)
# unbabel-comet>=2.0.0  # For COMET score (requires GPU for best performance)

# Optional: For GPU support (uncomment if you have CUDA)
# torch==2.0.0+cu118
# torchaudio==2.0.0+cu118

# Development dependencies (optional)
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0

# For building .exe
# pyinstaller>=5.13.0